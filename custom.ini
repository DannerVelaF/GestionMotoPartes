; Aumentar límite de subida de archivos (ej. 64MB)
upload_max_filesize = 64M
post_max_size = 64M

; Aumentar memoria para procesar imágenes pesadas
memory_limit = 256M

; Aumentar tiempo de ejecución (por si el internet es lento)
max_execution_time = 120